---
description: Recover the current Dev Agent OPC phase and next executable flow brief
---

Use this as `/opc-next <project-name>`.

Run `bin/dev-flow status <project-name>` and `bin/dev-flow next <project-name>`. Load only the files named in the brief, then continue with the listed command file, skills, outputs, gate, and follow-up `phase` command.

Prefer project-local `bin/dev-flow`; otherwise use the installed `dev-agent-opc-runtime/bin/dev-flow`.
