# Delegation Plan: {{PROJECT}}

Records subagent task packets suggested by `bin/dev-flow delegate {{PROJECT}}`
or the `Parallelizable work` section of `bin/dev-flow next {{PROJECT}}`.
Subagents are optional host capabilities; the main agent keeps ownership of
integration and gates.

## Current Policy
- SUBAGENTS:
- SUBAGENT_MODE:
- MAX_PARALLEL_SUBAGENTS:

## Active Task Packets
| Task id | Role | Goal | Write scope | Status | Notes |
|---|---|---|---|---|---|

## Packet Template
Create concrete packets under `tasks/subagents/<task-id>.md` when delegation is
actually used.
